package com.example.solarradarapp.ui.theme

import androidx.compose.ui.graphics.Color

// Backgrounds
val BackgroundApp = Color(0xFF0B1220)
val BackgroundNav = Color(0xFF111827)
val BackgroundContent = Color(0xFFF0F4F8)
val BackgroundCard = Color.White
val BackgroundTopBar = Color.White

// Brand
val PrimaryBlue = Color(0xFF3B82F6)
val LightBlue = Color(0xFF60A5FA)
val NavActiveIndicator = Color(0xFF60A5FA).copy(alpha = 0.14f)

// Text
val TextPrimary = Color(0xFF1A1A1A)
val TextSecondary = Color(0xFF8A8A8A)
val TextOnDark = Color.White
val TextOnDarkSecondary = Color.White.copy(alpha = 0.72f)
val TextOnDarkMuted = Color.White.copy(alpha = 0.42f)
val NavIconInactive = Color.White.copy(alpha = 0.48f)

// Status
val StatusSuccess = Color(0xFF2E7D32)
val StatusWarning = Color(0xFFED6C02)
val StatusError = Color(0xFFD32F2F)
val StatusInfo = Color(0xFF0288D1)
val StatusDefault = Color(0xFF757575)

// Dividers
val DividerLight = Color(0x1F000000)
val DividerDark = Color(0x14FFFFFF)

// Logout
val LogoutRed = Color(0xFFEF4444).copy(alpha = 0.70f)
